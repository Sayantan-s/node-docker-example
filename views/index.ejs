<%- include('includes/layout.ejs') %>
<body>
    <%- include('includes/navigation.ejs') %>
        <% if(product.length > 0) { %>
            <% product.forEach(item => { %>
                <h1>
                    <%= item.title%>
                </h1>
            <%})%>
        <% } else { %>

        <% } %>
       
</body>
</html>